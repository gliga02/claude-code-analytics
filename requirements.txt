# Runtime dependencies. Keep in sync with pyproject.toml [project.dependencies].
# Install: pip install -r requirements.txt
# Editable dev install (tests): pip install -e ".[dev]" or pip install -r requirements-dev.txt
pandas>=2.0
streamlit>=1.28
sqlalchemy>=2.0
plotly>=5.18
scikit-learn>=1.3
